#include <stdio.h>
/**
 * main - Hexadecimal
 * Return: Always 0
 */
int main(void)
{
	int n;
	int m;

	for (n = 48; n <= 58; n++)
	{
		putchar(n);
	}
	for (m = 97; m <= 103; n++)
	{
		putchar(m);
	}
	putchar('\n');
	return (0);
}
